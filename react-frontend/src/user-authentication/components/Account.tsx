import {UserDetails} from "./userDetails";
import {GridStyles} from "../styles/Grid";

export const Account: React.FC = () => {
    return (
        <GridStyles>
            <UserDetails />
        </GridStyles>
    );
}
